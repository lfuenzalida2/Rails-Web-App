<p id="notice"><%= notice %></p>

<h1>Locals</h1>

<div class="jumbotron">
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Direction</th>
      <th>Type</th>
      <th>Nº Dates</th>
      <th colspan="4"> Actions </th>
    </tr>
  </thead>

  <tbody>
    <% @locals.each do |local| %>
      <tr>
        <td><%= local.nombre %></td>
        <td><%= local.direccion %></td>
        <td><%= local.tipo %></td>
        <% if local.n_citas %>
          <td><%= local.n_citas %></td>
        <% else%>
          <td>0</td>
        <% end %>
        <td><%= link_to  'See Details', local_path(local.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Create Local', locals_new_path, class: "btn btn-primary" %>
</div>